// Custom JavaScript for Micro Real Estate Management

document.addEventListener('DOMContentLoaded', function() {
    console.log('MicroREMS JavaScript loaded!');

    // Example: Add a smooth scroll to anchor links
    // document.querySelectorAll('a[href^="#"]').forEach(anchor => {
    //     anchor.addEventListener('click', function (e) {
    //         e.preventDefault();
    //         document.querySelector(this.getAttribute('href')).scrollIntoView({
    //             behavior: 'smooth'
    //         });
    //     });
    // });

    // Example: Initialize Bootstrap tooltips (if you use them)
    // var tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="tooltip"]'))
    // var tooltipList = tooltipTriggerList.map(function (tooltipTriggerEl) {
    //   return new bootstrap.Tooltip(tooltipTriggerEl)
    // })

});

// You can add more specific functions below

// function exampleFunction() {
//     // ...
// }